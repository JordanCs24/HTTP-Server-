// Only declare what you NEED:
class HttpServer {
public:
    // Constructor, destructor
    // start() - creates socket, binds, listens
    // run() - main loop
private:
    // Member variables you'll need
};